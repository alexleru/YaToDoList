<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.YaToDoList" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/back_primary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/back_secondary</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >@color/back_primary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="CheckBox" parent="Theme.AppCompat.Light">
        <!--<item name="colorControlNormal">@android:color/holo_green_light</item>-->
        <item name="colorControlActivated">@color/green</item>
    </style>

    <style name="YaToDoList.DatePickerDialog" parent="Theme.AppCompat.Light.Dialog">
    </style>
</resources>